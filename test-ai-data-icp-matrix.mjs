import assert from 'node:assert/strict';
import fs from 'node:fs';
import Matrix from './ai-data-icp-matrix.js';
import Columns from './keyword-columns.js';

assert.equal(Matrix.data.revision,'ai-data-positioning-matrices-v3');
assert.equal(Matrix.data.icpColumns.length,12);
assert.deepEqual([...new Set(Matrix.data.icpColumns.map(c=>c.matrixGroup))],['ind','ctry','tech']);
assert.equal(Matrix.data.valueColumns.length,6);
assert.deepEqual(Matrix.data.valueColumns.map(c=>c.matrixGroup),['use','use','use','use','benefit','cap']);
assert.equal(Matrix.data.icpRows.length,5);assert.equal(Matrix.data.valueRows.length,5);
assert.equal(Matrix.data.icpRows.flatMap(r=>r.cells).length,60);assert.equal(Matrix.data.valueRows.flatMap(r=>r.cells).length,30);
assert.match(Columns.strategyPrompt,/Processes and use cases belong to Value/);
assert.match(Columns.strategyPrompt,/named competitor/);
assert.ok(Columns.names.category.includes('Integration pages'));

const use=Matrix.data.columns.slice(0,4),rest=Matrix.data.columns.slice(4);
const icpRows=Matrix.data.rows.map(row=>({id:row.id,pageGroup:'matrix',name:row.name,cells:Object.fromEntries(Matrix.data.columns.map((col,i)=>[col.id,{v:row.cells[i].title,mode:'aeo',cfg:true}]))}));
icpRows[0].cells[use[0].id].v='Preserved moved use case';icpRows[0].cells[rest[0].id].v='Preserved ICP edit';
const seoCell={v:'Preserve SEO',mode:'seo',cfg:true};const aeoCell={v:'Clear old Value AEO',mode:'aeo',cfg:true};
const content={views:{icp:{kind:'grid',columns:[],pageColumns:{matrix:structuredClone(Matrix.data.columns)},pageOrders:{},rows:icpRows},value:{kind:'grid',columns:[],pageColumns:{informational:[{id:'guide'}]},pageOrders:{},rows:[{id:'legacy',pageGroup:'informational',cells:{seo:seoCell,aeo:aeoCell}}]},category:{kind:'grid',columns:[],pageColumns:{landing:[]},pageOrders:{},rows:[]}}};
assert.equal(Matrix.ensure(content,'AI Data Platform','0jgsw8bx554d'),true);
assert.equal(content.views.icp.pageColumns.matrix.length,12);
assert.equal(content.views.value.pageColumns.matrix.length,6);
assert.equal(content.views.icp.rows.find(r=>r.id===Matrix.data.rows[0].id).cells[rest[0].id].v,'Preserved ICP edit');
assert.equal(content.views.value.rows.find(r=>r.id===Matrix.data.rows[0].id).cells[use[0].id].v,'Preserved moved use case');
assert.equal(content.views.value.rows.find(r=>r.id==='legacy').cells.seo,seoCell);
assert.equal(content.views.value.rows.find(r=>r.id==='legacy').cells.aeo,undefined);
assert.ok(content.views.category.pageColumns.landing.some(c=>c.name==='Integration pages'));
assert.match(content.views.icp.matrixAiPrompt,/audience-fit/);assert.match(content.views.value.matrixAiPrompt,/Nodal benefits/);
assert.equal(Matrix.ensure(content,'AI Data Platform','0jgsw8bx554d'),false);
const html=fs.readFileSync(new URL('./index.html',import.meta.url),'utf8');
for(const marker of ['Value matrix','matrix-ai-prompt','Nodal benefits','Capabilities','isPositioningMatrix','ai-data-icp-matrix.js'])assert.ok(html.includes(marker),marker);
console.log('PASS: ICP audience-fit matrix retained, process/use-case cells moved to Value with nodal benefits and capabilities, old Value AEO cleared, SEO preserved, category integration added, prompts embedded.');
